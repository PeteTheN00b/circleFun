#ifndef CIRCPOINT_HPP
#define CIRCPOINT_HPP

struct circPoint
{
	//might need to make custom constructor later, so angle is always given with mod360
	float angle_ = 0;
	float strength_ = 30;
};



#endif